import { ExplainOptions } from '../explain';
import InteractionHistory from '../interaction-history';
import { Agent } from '../agent';
import { ProjectInfo } from '../project-info';
import { HelpProvider } from '../help';
import VectorTermsService from './vector-terms-service';
import LookupContextService from './lookup-context-service';
import ApplyContextService from './apply-context-service';
type AgentModeResult = {
    agent: Agent;
    question: string;
};
export default class AgentSelectionService {
    private history;
    private helpProvider;
    private vectorTermsService;
    private lookupContextService;
    private applyContextService;
    constructor(history: InteractionHistory, helpProvider: HelpProvider, vectorTermsService: VectorTermsService, lookupContextService: LookupContextService, applyContextService: ApplyContextService);
    selectAgent(question: string, options: ExplainOptions, projectInfo: ProjectInfo[]): AgentModeResult;
}
export {};
