import { ContextResponse } from '../context';
import InteractionHistory from '../interaction-history';
import { HelpResponse } from '../help';
export default class ApplyContextService {
    readonly interactionHistory: InteractionHistory;
    constructor(interactionHistory: InteractionHistory);
    applyContext(context: ContextResponse | undefined, help: HelpResponse, characterLimit: number): void;
    addSystemPrompts(context: ContextResponse, help: HelpResponse): void;
}
