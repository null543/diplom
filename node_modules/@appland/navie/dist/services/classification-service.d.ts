import InteractionHistory from '../interaction-history';
export default class ClassificationService {
    readonly interactionHistory: InteractionHistory;
    modelName: string;
    temperature: number;
    constructor(interactionHistory: InteractionHistory, modelName: string, temperature: number);
    classifyQuestion(question: string): Promise<string>;
}
