import InteractionHistory from '../interaction-history';
export type Completion = AsyncIterable<string>;
export default interface CompletionService {
    complete: () => Completion;
}
export declare class OpenAICompletionService implements CompletionService {
    readonly interactionHistory: InteractionHistory;
    readonly modelName: string;
    readonly temperature: number;
    constructor(interactionHistory: InteractionHistory, modelName: string, temperature: number);
    complete(): Completion;
}
