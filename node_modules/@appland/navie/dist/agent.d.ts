import { ProjectInfo } from './project-info';
export declare enum AgentMode {
    Explain = "explain",
    Generate = "generate",
    Help = "help"
}
export declare class AgentOptions {
    question: string;
    aggregateQuestion: string;
    chatHistory: string[];
    projectInfo: ProjectInfo[];
    codeSelection?: string | undefined;
    constructor(question: string, aggregateQuestion: string, chatHistory: string[], projectInfo: ProjectInfo[], codeSelection?: string | undefined);
    get hasAppMaps(): boolean;
}
export interface Agent {
    perform(options: AgentOptions, tokensAvailable: () => number): Promise<void>;
    applyQuestionPrompt(question: string): void;
}
